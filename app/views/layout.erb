<!DOCTYPE html>
 <html lang="en"> 
  <head>
    <meta charset="utf-8" />
    <title>ShoppingList</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/main.css" />
  </head>
  <body>
    <div class='wrapper'>
    <% if logged_in? %>
       Welcome, <%= "#{current_user.username}" %>!
       <div class="container">
      <a href="/logout">Sign Out</a><br>
      <a href="/items/new">Build new Item</a>
      </div>
    <% else %>
      <h1>Welcome!<h1>
      <div class="logs">
      <a href="/signup">Sign up</a>
      </div>
      <div class="logs">
      <a href="/login">Log In</a><br>
      </div>
    <% end %>
    <%= yield %>
      <footer class='branding'>
        <small>&copy; 2020 <strong>ShoppingList</strong></small>
      </footer>
    </div>
  </body>
</html>
